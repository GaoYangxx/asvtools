if (interactive()) {
  suppressMessages(require(devtools))
}

options(
  usethis.description = list(
    "Authors@R" = utils::person(
      "Yang", "Gao",
      email = "2421078944gy@gmail.com",
      role = c("aut", "cre")
      # You can add a comment with ORCID here if you have one, like:
      # comment = c(ORCID = "YOUR_ORCID_GOES_HERE")
    ),
    License = "MIT + file LICENSE"
  )
)

PATH <- "D:/game/rtools45/usr/bin;D:/game/rtools45/bin;%PATH%"
